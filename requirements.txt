# Douyin Reach 后端依赖
# Python 3.10+（建议 3.10–3.12，3.14 需编译 greenlet 等）

# Web 服务器
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# 浏览器自动化
playwright>=1.40

# 工具库
python-dotenv>=1.0.0
cryptography>=42.0.0

# 可选：桌面壳（如果需要 pywebview 模式）
pywebview>=4.0
