# 项目运行指南

## 快速启动

### 方式一：使用启动脚本（推荐）
双击运行项目根目录下的 `启动应用.bat`

### 方式二：命令行启动
```bash
cd F:\AI+program\douyin_ass
python main.py
```

## 启动模式

### 生产模式（默认）
- 使用已构建的前端文件（dist/index.html）
- 适合日常使用
- 前提：已运行过 `cd src/frontend && npm run build`

### 开发模式
1. 先启动前端开发服务器：
   ```bash
   cd src/frontend
   npm run dev
   ```

2. 在另一个终端启动后端：
   ```bash
   set DOUYIN_REACH_DEV=1
   python main.py
   ```

## 首次使用步骤

1. **启动应用**
   - 双击 `启动应用.bat` 或运行 `python main.py`
   - 应用窗口会自动打开

2. **登录抖音**
   - 进入「系统设置」页面
   - 点击「打开登录浏览器」
   - 使用抖音扫码完成登录
   - Cookie 会自动保存到 `data/douyin_storage_state.json`

3. **创建任务**
   - 进入「任务管理」页面
   - 点击「创建任务」
   - 填写任务名称、关键词、规则等
   - 保存任务

4. **开始采集**
   - 在任务列表中点击「启动采集」
   - 进入「采集监控」查看实时进度
   - 等待采集完成

5. **筛选用户**
   - 采集完成后，点击「执行筛选」
   - 系统会根据规则筛选目标用户

6. **审核名单**
   - 进入「名单审核」页面
   - 勾选要发送私信的用户
   - 点击「确认发送」

7. **发送私信**
   - 进入「发送控制」页面
   - 点击「开始发送」
   - 查看发送进度和结果

## 常见问题

### 1. 窗口无法启动
**原因**: pywebview 依赖问题

**解决方案**:
```bash
# 安装 Microsoft C++ 生成工具
# 下载地址: https://visualstudio.microsoft.com/visual-cpp-build-tools/

# 重新安装依赖
pip install -r requirements.txt
```

### 2. 提示未登录
**原因**: Cookie 过期或未登录

**解决方案**:
- 进入「系统设置」重新登录
- 删除 `data/douyin_storage_state.json` 后重新登录

### 3. 采集无响应
**原因**: 会话检查失败

**解决方案**:
```bash
# 运行诊断脚本
python scripts/diagnose_session.py

# 查看详细日志
cat logs/*.log
```

### 4. 前端页面空白
**原因**: 前端未构建

**解决方案**:
```bash
cd src/frontend
npm install
npm run build
```

## 配置说明

### 环境变量
- `DOUYIN_REACH_DEV=1` - 开发模式
- `DOUYIN_REACH_MOCK=1` - 使用 Mock 后端
- `DOUYIN_REACH_CDP_PORT=9222` - 开启 CDP 调试端口

### 配置文件
- `data/settings.json` - 应用配置
- `data/douyin_storage_state.json` - 登录 Cookie
- `data/douyin_reach.db` - 数据库

## 日志文件

- `logs/` - 所有日志文件
- `debug-*.log` - 调试日志
- `acceptance-*.log` - 验收测试日志
- `cdp-pages-*.log` - CDP 监控日志

## 技术支持

如遇到问题，请查看：
1. `docs/` 目录下的详细文档
2. `docs/修复报告-会话检查-20260227.md` - 最新修复说明
3. `docs/09-项目经验与关键信息.md` - 常见问题汇总

## 修复记录

### 2026-02-27: 会话检查修复
- **问题**: 抖音 API 格式变化导致会话检查失败
- **修复**: 更新 `_check_session_no_navigate()` 方法
- **状态**: ✓ 已修复并验证
- **详情**: 见 `docs/修复报告-会话检查-20260227.md`
